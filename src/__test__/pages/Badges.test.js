import {render, screem} from '@testing-library/react'
import Badges from '../../pages/Badges'

describe('<Home />', () => {
  
  test('renders content', () => {
    render(<Badges />)
    
  })
  
})
